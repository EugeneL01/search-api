import SearchBar from "./components/SearchBar";

const Home = async () => {

  return (
    <main className="min-h-screen">
      <div className="px-[30px] pt-[40px]">
        <SearchBar />
      </div>
    </main>
  );
};

export default Home;
